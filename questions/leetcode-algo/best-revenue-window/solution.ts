/**
 * Largest sum of a non-empty contiguous window of `deltas`.
 * Returns 0 for an empty input.
 */
export function bestRevenueWindow(deltas: number[]): number {
  // TODO: implement — delete the throw below and build the real thing.
  void deltas;
  throw new Error('bestRevenueWindow() is not implemented yet');
}
