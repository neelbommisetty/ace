export default function App() {
  const style = { padding: 16, fontFamily: 'system-ui' };
  return (
    <div style={style}>
      <h1>Scratch pad</h1>
      <p>Edit App.tsx — the preview hot-reloads on save.</p>
    </div>
  );
}
